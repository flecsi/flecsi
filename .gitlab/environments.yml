#------------------------------------------------------------------------------#
# FleCSI Gitlab CI: Environments
#------------------------------------------------------------------------------#

include: .gitlab/templates.yml

mpich:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: mpich
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: centos-8
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5 +kokkos

mpich-debug:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: mpich
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: centos-8
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5 +kokkos ^legion build_type=Debug

mpich-clang:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: mpich
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: clearlinux
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5 +kokkos

mpich-clang-debug:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: mpich
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: clearlinux
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5 +kokkos ^legion build_type=Debug

openmpi:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: openmpi
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: centos-8
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5

openmpi-debug:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: openmpi
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: centos-8
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5 ^legion build_type=Debug

openmpi-clang:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: openmpi
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: clearlinux
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5

openmpi-clang-debug:
  extends: .env_template
  dependencies: []
  only:
    - merge_requests
  variables:
    ENVIRONMENT: '${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    MPI_PROVIDER: openmpi
    REGISTRY: gitlab.lanl.gov:5050/flecsi/flecsi
    IMAGE: clearlinux
    VERSION: flecsi@'${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}'
    VARIANTS: +hdf5 ^legion build_type=Debug
